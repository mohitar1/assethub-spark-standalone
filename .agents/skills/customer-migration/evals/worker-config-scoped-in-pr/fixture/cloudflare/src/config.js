const config = {
  // Demo customer scope: assets are filtered to assetMetadata.company === DEMO_COMPANY.
  DEMO_COMPANY: 'frescopa',
  // Portal routing/login base ('/<companyKey>'); '' = repo root (production default).
  DEMO_BASE_PATH: '',
};

export default config;
